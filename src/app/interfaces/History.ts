export interface History {
    historyId: number;      // Corresponds to int
    userId: number;         // Corresponds to int
    requestIP: string;      // Corresponds to string
    timestamp: Date;        // Corresponds to DateTime
    status: boolean;        // Corresponds to bool
}
