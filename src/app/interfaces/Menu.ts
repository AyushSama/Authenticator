export interface Menu {
    menuId: number;          // Corresponds to MenuId
    menuName: string;       // Corresponds to MenuName
    parentId: number;       // Corresponds to ParentId
    menuApiUrl: string;    // Corresponds to MenuApiUrl (optional)
}
