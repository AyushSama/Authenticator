export interface User {
    userId: number;           // Represents the user's ID
    email: string;            // Represents the user's email
    password: string;         // Represents the user's password
    createdDate: Date;       // Represents the date the user was created
  }